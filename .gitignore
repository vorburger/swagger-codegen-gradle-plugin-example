# TODO should not need this, because should be build/swagger instead of src/swagger, by default!
src/swagger

.gradle
build/
bin/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# Eclipse
.settings/
.project
.classpath
